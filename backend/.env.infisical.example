# ── Secrets that should be stored in Infisical ─────────────────
# Copy these keys into your Infisical project.
# The backend fetches them at runtime when INFISICAL_CLIENT_ID is configured.

AZURACAST_API_KEY=
JWT_SECRET=
PANEL_SECRET=
WORKER_AUTH_SECRET=
WEBHOOK_SECRET=
FB_VERIFY_TOKEN=
FB_APP_SECRET=
