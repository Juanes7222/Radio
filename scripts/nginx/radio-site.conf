# =============================================================================
# radio-site.conf — Nginx entry point for the radio stack
#
# Installed to /etc/nginx/sites-available/radio (symlinked into sites-enabled)
# by setup.sh / deploy.sh. The full config tree lives in /etc/nginx/radio/
# (domains/ + snippets/), installed from scripts/nginx/ by the same scripts.
# =============================================================================

include /etc/nginx/radio/domains/*.conf;
