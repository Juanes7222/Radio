# =============================================================================
# snippets/proxy-websocket.conf — WebSocket-capable proxy block
#
# Uses the $connection_upgrade map (radio-global.conf) so the Connection
# header is only rewritten when the client actually requests an upgrade.
# Long read timeout keeps long-lived sockets open.
# =============================================================================

proxy_http_version 1.1;
proxy_set_header   Upgrade           $http_upgrade;
proxy_set_header   Connection        $connection_upgrade;
proxy_set_header   Host              $host;
proxy_set_header   X-Real-IP         $remote_addr;
proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header   X-Forwarded-Proto $scheme;
proxy_buffering    off;
proxy_cache        off;
proxy_read_timeout 3600s;
