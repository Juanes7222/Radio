/**
 * Re-exporta desde @radio/api (paquete compartido del monorepo).
 * Los imports existentes en esta app siguen funcionando sin cambios.
 */
export { useAzuraCast } from '@radio/api';
export type { UseAzuraCastProps, UseAzuraCastReturn } from '@radio/api';
